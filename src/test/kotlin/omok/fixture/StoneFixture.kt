package omok.fixture

import omok.model.Color
import omok.model.Point
import omok.model.Stone

val FIRST_ROW_FIRST_COL = Stone(Point(1, 1), Color.BLACK)
val FIRST_ROW_SECOND_COL = Stone(Point(1, 2), Color.BLACK)
