package omok.model

class Players(vararg player: Player) {

    /*private val players = player.toList()

    fun playGame(putStone: (Color) -> Stone) {
        while (players.any{it.checkContinuity(putStone())})
    }*/
}
